# Exercises – Tuples

**Easy**
1. Create a tuple with three different data types and print each element.
2. Write a function that returns both the sum and product of two numbers as a tuple.
3. Access the last element of a tuple using negative indexing.

**Medium**
4. Given a list of numbers, convert it to a tuple and determine if it contains duplicates.
5. Unpack a tuple of the form (year, month, day) into separate variables.
6. Use a tuple as a key in a dictionary to map a coordinate to a city name.

**Hard**
7. Implement a function zip_longest_tuple(*iterables, fillvalue=None) that mimics `itertools.zip_longest` but returns a tuple of tuples.
8. Write a recursive function that flattens a nested tuple of arbitrary depth into a flat tuple.
9. Create a tuple comprehension (using a generator expression) that yields the squares of numbers 0‑9.
10. Design a simple immutable stack class using tuples for internal storage.
