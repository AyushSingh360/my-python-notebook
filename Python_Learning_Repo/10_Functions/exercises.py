# Exercises – Functions

**Easy**
1. Write a function `is_even(n)` returning True if even.
2. Function `area_circle(r)` returning area (pi*r^2).
3. Function `say_hello()` that prints a greeting.

**Medium**
4. `max_of_three(a, b, c)` returning the largest.
5. `count_vowels(s)` returning integer count.
6. Higher-order `apply_twice(func, arg)` calling func(func(arg)).

**Hard**
7. Recursive `flatten(lst)` for arbitrarily nested lists.
8. Decorator `@timer` that prints execution time.
9. `group_by_first_letter(words)` returning a dict.
10. Command dispatcher `run(cmd, *args)` using a dict mapping.
