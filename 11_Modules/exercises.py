# Exercises – Modules

**Easy**
1. Create `constants.py` with `PI = 3.14`; import it in another script.
2. `greetings.py` with `morning(name)`; use it.
3. Use `random` module to pick a number 1-100.

**Medium**
4. Build a package `calc` with `add.py` and `sub.py`.
5. Class `Counter` in `counter.py`; import and use.
6. Check `if __name__ == "__main__"` behavior by importing a script.

**Hard**
7. Lazy load a heavy module only inside a function.
8. Plugin system: iterate `.py` files in a folder and dynamically import.
9. Absolute vs relative imports in a nested package structure.
10. Measure import time of standard modules.
