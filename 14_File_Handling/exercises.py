# Exercises – File Handling

**Easy**
1. Write "Hello World" to `hello.txt`.
2. Read `hello.txt` and print it.
3. Append "Goodbye" to `hello.txt`.

**Medium**
4. Create `employees.csv` with columns `id,name,salary`; write 3 rows.
5. Read `employees.csv` and calculate average salary.
6. Function `read_head(path, n)` returning first n lines.

**Hard**
7. Context Manager `SafeOpen` logging open/close events.
8. Copy a binary file chunk by chunk (1KB buffer).
9. Rotating log writer: switch file if size > 1KB.
10. Word frequency counter acting on a large file line-by-line.
