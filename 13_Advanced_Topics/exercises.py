# Exercises – Advanced

**Easy**
1. Cubes via list comprehension.
2. Lambda returning larger of two numbers.
3. Catch KeyError in dict access.

**Medium**
4. Dict comprehension: char -> frequency in "abracadabra".
5. Generator `evens(n)` yielding even numbers.
6. Raise custom `InvalidAgeError` for negative input.

**Hard**
7. Decorator `retry(times)` that repeats function on failure.
8. `reduce` to compute factorial.
9. Class `PrimeGenerator` implementing iterator protocol.
10. Combine `map/filter`: strings > length 3 to uppercase.
